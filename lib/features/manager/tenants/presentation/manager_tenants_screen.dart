import 'package:flutter/material.dart';

class ManagerTenantsScreen extends StatelessWidget {
  const ManagerTenantsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Manager Tenants')));
  }
}
